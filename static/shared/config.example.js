/**
 * Music Theory Games — Configuration Template
 * shared/config.example.js
 *
 * API keys are no longer stored in client-side JS.
 * Set ANTHROPIC_API_KEY as a server environment variable instead.
 * See .env.example at the project root.
 */
